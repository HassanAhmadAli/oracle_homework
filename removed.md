## 3. Build a `MATERIALIZED VIEW` that includes the `Customer Name` and the `Reservation Location`. The data in this view must update `incrementally`

## 3. Show the `Number of Reservations` grouped by `(Location, Occasion, Customer)` and also by `(Location, Occasion)`



## 5. The company manager wants to know `which Occasions and Reservation Locations are correlated` (occur together) to use this information for estimating new occasions. `Suggest a suitable method` to find this relationship, then `implement it using Weka`
