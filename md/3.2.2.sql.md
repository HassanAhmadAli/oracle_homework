```plsql

CREATE TABLE Fact_Reservations_Composite (
    FactID NUMBER,
    TimeID NUMBER,
    LocationID NUMBER,
    ReservationDate DATE,
    Duration_Minutes NUMBER
) PARTITION BY RANGE (ReservationDate) SUBPARTITION BY LIST (LocationID) (
    PARTITION p_2023
    VALUES LESS THAN (TO_DATE('01-01-2024', 'DD-MM-YYYY')) (
            SUBPARTITION p_2023_loc1
            VALUES (1),
                SUBPARTITION p_2023_loc2
            VALUES (2),
                SUBPARTITION p_2023_other
            VALUES (DEFAULT)
        ),
        PARTITION p_2024
    VALUES LESS THAN (TO_DATE('01-01-2025', 'DD-MM-YYYY')) (
            SUBPARTITION p_2024_loc1
            VALUES (1),
                SUBPARTITION p_2024_loc2
            VALUES (2),
                SUBPARTITION p_2024_other
            VALUES (DEFAULT)
        )
);
/
```
